---
  - hosts: all
    become: true
    tasks:
      - name: Replace a line in a file
        ansible.builtin.lineinfile:
          path: /etc/somefile.conf
          regexp: '^#Ext.Msg.show({'
          line: 'void({ //Ext.Msg.show({'
